<div class="container">
  <div class="row">
    <div class="col-md-8 mx-auto post-form-container">
      <h2 class="h2">トピック新規投稿</h2>
      <%= form_with(url: create_topics_path, local: true, method: :post, html: { class: 'post-form' } ) do |f| %>

        <div class="form-group">
          <%= f.label "トピック名", class: 'form-label' %>
          <%= f.text_field :title, class: 'form-control' %>
        </div>

        <div class="button-group">
          <%= f.submit '投稿する', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>